{
  "name": "ArrayWithMixed",
  "version": "1.0",
  "description": "a simple structure with an array that may contain mixed values",
  "fields": {
    "arr": {
      "name": "arr",
      "type": "array",
      "description": "list of mixed types",
      "values": [
        {
          "type": "string",
          "description": "some string value"
        },
        {
          "type": "object",
          "description": "When a string just doesn't cut it...",
          "fields": [
            {
              "type": "number",
              "description": "the baz"
            },
            {
              "type": "boolean",
              "description": "the quux"
            }
          ]
        }
      ]
    }
  }
}